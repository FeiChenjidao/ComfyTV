# Layer Separation workflows

Place `*_preset.json` (+ matching API/GUI JSON) here. Each workflow should
save a **layered PSD/PSB** (or image batch) as its result.

In ComfyTV workflow config, set **Result type** to **Layered image**
(`ui_save_layered`) and point **Result node** at the save node.
